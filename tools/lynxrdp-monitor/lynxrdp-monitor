#!/usr/bin/env python3
"""Launcher, so the viewer can be run straight from a checkout."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from lynxrdp_monitor.app import main  # noqa: E402

raise SystemExit(main())
